# EduRAG 教育知识库问答系统 - 产品需求文档PRD

> 文档版本：V1.0 
>
> 编写日期：2026‑08‑27 
>
> 项目简介：面向教育场景的本地 RAG 知识库问答后端系统，支持多格式文档上传入库、向量检索、重排、大模型智能答疑，基于 FastAPI + Milvus + MySQL + Redis + MinIO 技术栈。

## 1. 项目概述

### 1.1 背景

​		教育场景存在大量课件、教案、习题文档（pdf、docx、ppt、txt、md），教师 / 学生需要快速从大量资料中获取知识点。传统关键词搜索无法理解语义，因此搭建一套 RAG 知识库问答后端。 系统接收文档文件，完成解析、文本切分、向量化存入向量库；用户提问时，通过向量语义检索 + 重排，把相关文档片段送入大模型，基于私有知识库给出答案，减少大模型幻觉。

### 1.2 技术栈

- Web 框架：FastAPI
- 关系数据库：MySQL（存储文档元数据）
- 缓存：Redis（向量缓存、会话缓存）
- 对象存储：MinIO（保存原始上传文档）
- 向量数据库：Milvus（存储文本向量，语义检索）
- Embedding 模型：bge‑small‑zh‑v1.5（本地离线）
- Rerank 重排模型：bge‑reranker‑v2‑m3（本地离线）
- LLM 大模型：阿里云通义千问兼容接口 (qwen3.7‑plus)

## 2. 产品目标

1. 支持多种格式教学文档上传，自动完成解析、切片、向量化入库。
2. 基于私有知识库做语义问答，大模型严格依赖参考文档作答，禁止编造不存在内容。
3. 支持多知识库隔离，不同知识库之间数据互不干扰。
4. 具备完整日志、异常处理、文件完整性校验，保障系统稳定性。
5. 架构具备可扩展性，支持后续扩展 OCR、多轮对话、流式输出、权限管理。

## 3. 用户角色

|          角色           |                          说明                          |
| :---------------------: | :----------------------------------------------------: |
|         管理员          | 上传教学文档、维护知识库，调用上传接口，管理知识库内容 |
| 终端用户（教师 / 学生） |      发起问答请求，基于已上传知识库获取知识点解答      |

> 当前版本未实现鉴权，所有调用方默认视为可信调用方；迭代版本增加 JWT 身份认证。

## 4. 业务流程

### 4.1 文档入库流程

~~~
前端上传文件 → 后端校验文件 → 临时存储 → MD5完整性计算 → MinIO存储原始文件
→ 执行知识库流水线：MinIO下载文件→文档解析→文本分片→批量向量化→向量存入Milvus→元数据写入MySQL
→ 清理服务器本地临时文件 → 返回文档ID、分片数量
~~~

### 4.2 知识库问答流程

~~~
用户提交问题 → 问题向量化 → Milvus向量粗召回候选片段
→ Reranker重排打分、过滤低相关性片段 → 拼接参考上下文
→ 组装System提示词+上下文+用户问题 → 请求大模型
→ 返回大模型生成答案
~~~

## 5. 功能需求

### 5.1 文档知识库管理

> 当前版本现状：仅支持传入 kb_id，数据库无独立知识库表

-  迭代：创建、查询、删除知识库，每个知识库分配唯一 kb_id。
-  迭代：知识库权限控制，不同用户只能访问授权知识库。
-  迭代：删除知识库时，同步删除 MySQL 元数据、MinIO 原始文件、Milvus 向量数据。

### 5.2 文档上传与解析入库

|      功能点      |                           需求描述                           | 当前版本实现状态 |
| :--------------: | :----------------------------------------------------------: | :--------------: |
|   文件格式校验   | 支持 `.txt` `.md` `.pdf` `.docx` `.ppt` `.pptx`，其他格式拒绝上传 |     ✅已实现      |
|  文件完整性校验  | 计算文件 MD5，上传 MinIO、下载后校验 MD5，检测文件损坏 / 篡改 |     ✅已实现      |
| 对象存储原始文档 | 原始文件持久化保存至 MinIO，按`kb_{知识库ID}/UUID‑原文件名`路径存储 |     ✅已实现      |
|  多格式文档解析  | 自动识别文件后缀调用对应解析器，提取文档纯文本；docx 读取表格；ppt 标记页码 |     ✅已实现      |
|   文本递归分片   | 使用中文递归分片器，chunk_size、overlap 可配置，尽量保证语义完整，避免句子截断 |     ✅已实现      |
|    批量向量化    | 使用本地 BGE 模型批量生成向量；Redis 缓存向量，相同文本复用向量减少推理开销 |     ✅已实现      |
| 向量存入 Milvus  |         将分片文本与向量存入向量库，用于后续语义检索         |     ✅已实现      |
| MySQL 保存元数据 | 记录文档所属知识库 ID、原文件名、MinIO 存储信息、MD5、分片数量、上传时间 |     ✅已实现      |
| 临时文件自动清理 |    无论成功失败，自动删除服务器本地临时文件，防止磁盘占满    |     ✅已实现      |
|    空文档拦截    | 文档解析后无有效文本，直接终止流水线并返回错误，不生成无效向量 |     ✅已实现      |
|   文件大小限制   |         限制上传文件最大大小，防止超大文件 DoS 攻击          |     ⚠️待完善      |
|     文件去重     |     根据 MD5 检测重复文件，重复文件跳过完整解析入库流程      |     ⚠️待完善      |
|   异步任务处理   | 大文档上传放入后台任务队列，接口不阻塞等待，返回任务 ID 轮询进度 |     ⚠️待完善      |

### 5.3 知识库智能问答

|      功能点       |                           需求描述                           | 当前版本实现状态 |
| :---------------: | :----------------------------------------------------------: | :--------------: |
|    向量粗召回     |       用户问题转为向量，Milvus 相似度检索召回候选片段        |     ✅已实现      |
|     重排过滤      | 使用 Cross‑Encoder 重排模型对召回片段打分，过滤低于阈值低相关片段 |     ✅已实现      |
| Prompt 约束大模型 | 系统提示词约束：优先使用参考上下文回答，无相关资料则回复知识库暂无相关内容，禁止编造 |     ✅已实现      |
|   LLM 调用重试    |           大模型接口调用失败自动重试，处理网络抖动           |     ✅已实现      |
|    知识库隔离     |     问答时根据 kb_id 过滤，只召回当前知识库内的文档片段      |     ⚠️待完善      |
|   流式问答输出    |                       SSE 流式返回答案                       |     ⚠️待完善      |
|     多轮对话      |                 支持上下文记忆，支持用户追问                 |     ⚠️待完善      |
|   检索结果缓存    |              相同问题复用检索结果，减少算力消耗              |     ⚠️待完善      |

## 6. 非功能需求

### 6.1 性能需求

1. 小文档（<10MB）上传入库响应合理；大文档需要改为异步任务，不能阻塞 HTTP 接口。
2. 普通问答单次全链路响应尽量控制在 5s 以内（受模型推理、大模型接口网络影响）。
3. Redis 缓存向量，降低重复文本向量化耗时。

### 6.2 可靠性需求

1. 文件全流程 MD5 校验，识别文件损坏、篡改。
2. LLM、MinIO 下载上传具备重试机制，应对瞬时网络抖动。
3. 临时文件必须清理，不能遗留磁盘垃圾。
4. 关键操作完整日志记录，便于故障排查。

### 6.3 安全需求

1. 生产环境密钥禁止明文存放在`.env`文件，使用系统环境变量注入。
2. 接口增加参数校验，防止超长输入、恶意请求。
3. 迭代版本增加身份鉴权，防止未授权访问知识库、上传文件。

### 6.4 可扩展性

1. 文档解析器、分片器、向量模型、LLM 客户端全部基于抽象基类实现，新增实现无需改动上层业务代码。
2. 配置集中管理，业务参数可通过环境变量修改，无需修改源码。

## 7. 接口定义

> BaseUrl：`http://127.0.0.1:8000`；自动生成接口文档地址 `/docs`

### 7.1 文档上传入库接口

- 请求地址：`POST /rag/upload_doc`
- 描述：上传文档文件，完成解析、切片、向量化入库
- Query 参数：
  - `kb_id:int`：知识库 ID，默认 1
- Body：form‑data，参数 `file` 上传文件
- 返回示例

~~~json
{
    "code":200,
    "msg":"文档入库成功，共生成XX条知识库片段",
    "file_id": 1,
    "chunk_count": 12,
    "file_key":"kb_1/xxxx‑xxx.pdf"
}
~~~

### 7.2 知识库问答接口

- 请求地址：`POST /rag/chat`
- 请求体

~~~json
{
    "question": "你的问题"
}
~~~

- 返回示例

~~~json
{
    "code":200,
    "msg":"请求成功",
    "data":{
        "answer":"大模型输出的回答内容"
    }
}
~~~

## 8. 数据设计

### 8.1 MySQL 表：knowledge_file（文档元数据表）

|      字段       |         类型          |       说明       |
| :-------------: | :-------------------: | :--------------: |
|       id        | bigint auto_increment |   主键文档 ID    |
|      kb_id      |          int          |  所属知识库 ID   |
| origin_filename |     varchar(255)      |    原始文件名    |
|     bucket      |     varchar(128)      |    MinIO 桶名    |
|   object_key    |     varchar(512)      |  MinIO 对象路径  |
|    file_md5     |      varchar(64)      |  文件 md5 指纹   |
|    chunk_num    |          int          | 文档拆分分片总数 |
|   create_time   |       datetime        |     上传时间     |

> 迭代新增表：知识库表、分片详情表、用户表、对话记录表。

### 8.2 Milvus 向量集合 `knowledge_collection`

| 字段   | 类型              | 说明                   |
| ------ | ----------------- | ---------------------- |
| id     | int64 auto_id     | 向量主键               |
| text   | varchar(4096)     | 文本分片内容           |
| vector | float_vector(512) | bge‑small‑zh‑v1.5 向量 |

> ⚠️ 当前缺少 kb_id 字段，迭代需要增加 kb_id 用于知识库过滤。

### 8.3 Redis 存储内容

1. `embed_cache:{md5}`：向量缓存，value 为逗号分隔的向量字符串，过期时间 24 小时。

### 8.4 MinIO 存储

存储全部上传原始教学文档，路径格式：`knowledge_{kb_id}/{uuid}-原文件名`。

## 9. 部署环境与依赖组件

### 9.1 中间件（通过 docker‑compose 部署）

- MySQL
- Redis
- MinIO
- Milvus

### 9.2 AI 模型文件（本地存放于项目`./models`）

- `bge‑small‑zh‑v1.5`
- `bge‑reranker‑v2‑m3`

### 9.3 环境配置文件 `.env`

包含服务端口、数据库连接、Milvus、MinIO、LLM 接口密钥、模型路径、重排阈值、业务参数。

### 9.4 启动步骤

1. 启动 docker‑compose 拉起全部中间件；
2. 执行`init_db.py`初始化 MySQL 数据库与数据表；
3. 将 BGE 模型放置到配置指定路径；
4. 配置`.env`；
5. 启动 FastAPI 服务。

## 10. 当前版本局限与迭代规划

### 10.1 V1.0 当前版本局限

1. 无独立知识库管理表，kb_id 仅作为参数传递，Milvus 没有 kb_id 过滤字段，问答会跨知识库召回。
2. 文件上传是同步 HTTP 接口，大文件会超时阻塞服务。
3. MySQL 为单连接，无连接池，高并发性能差。
4. 没有身份鉴权、权限控制。
5. 不支持流式问答、不支持多轮对话记忆。
6. Milvus 与 MySQL 无事务保障，存在数据不一致风险。
7. 缺少 OCR 能力，扫描版 PDF 无法解析文本。
8. 缺少文档删除能力，无法精准删除 Milvus 中指定文档对应的向量。

### 10.2 V1.1 迭代优先级规划

**P0（最高优先级）**

1. Milvus 增加 kb_id 字段，实现知识库数据隔离，检索增加过滤条件。
2. MySQL 引入连接池、ORM 框架，新增知识库表、分片元数据表。
3. 支持文档删除，可同步清理 MySQL、MinIO、Milvus 数据。
4. 实现全局异常处理器，统一接口返回格式。

**P1（重要功能）**

1. 引入 Celery 异步任务队列，文档上传改为异步处理，支持任务进度查询。
2. JWT 鉴权中间件，实现用户‑知识库权限控制。
3. 实现 SSE 流式问答接口。

**P2（体验增强）**

1. OCR 解析扫描 PDF / 图片文档。
2. Redis 实现多轮对话上下文记忆。
3. 文件 MD5 去重上传，避免重复解析向量化。
4. 请求链路追踪 ID，优化日志排查能力。

**P3 优化**

1. 支持切换云端 Embedding 服务。
2. 问答反馈接口，收集用户评价，用于调优阈值。